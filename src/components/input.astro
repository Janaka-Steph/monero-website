---
import { tv } from 'tailwind-variants'

const inputVariants = tv({
  base: 'flex h-12 w-full rounded-md bg-secondary px-3 py-2 text-sm placeholder:text-muted-foreground focus-visible:outline-none disabled:cursor-not-allowed disabled:opacity-50',
})

const { ...rest } = Astro.props
---

<input class={inputVariants()} {...rest} />
